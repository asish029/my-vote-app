        apiVersion: v1

        kind: Pod

        metadata: 
            name: postgres-pod
            lables:
              name: postgres-pod
              app: demo-voting-app 

        spec:
            container:
            - name: postgres-app
              image: postgres:9.4
              port:
              - containerPort: 5432