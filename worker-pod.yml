        apiVersion: v1

        kind: pod

        metadata: 
            name: worker-app-pod
            lables:
              name: worker-app-pod
              app: demo-voting-app 

        spec:
            container:
            - name: worker-app
              image: eesprit/voting-app-worker
              