        apiVersion: v1

        kind: Pod

        metadata: 
            name: result-app-pod
            lables:
              name: result-app-pod
              app: demo-voting-app 

        spec:
            containers:
            - name: result-app
              image: eesprit/voting-app-result
              port:
              - containerPort: 80