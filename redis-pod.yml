        apiVersion: v1

        kind: pod

        metadata: 
            name: redis-pod
            lables:
              name: redis-pod
              app: demo-voting-app 

        spec:
            container:
            - name: redis-app
              image: redis
              port:
              - containerPort: 6379